I built a RESTful API using Node.js and Express that connects to a PostgreSQL database hosted on Render.
Challenge: I had to manually configure the DBeaver connection using Host/Port details because the URL parser failed.
I used a .env file to hide my database password and added it to .gitignore.
